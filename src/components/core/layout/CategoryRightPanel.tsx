"use client";

import { usePathname } from "next/navigation";
import { getCategoryFromPath, DEMO_ACTIVITIES, ActivityItem } from "@/config/rightPanelConfig";

// Activity Feed Section - Empty
function ActivityFeed({ activities }: { activities: ActivityItem[] }) {
    return null;
}

// Main Category Right Panel - Empty
export function CategoryRightPanel() {
    return null;
}

